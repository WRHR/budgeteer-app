query findExpense(id:id) {
  id
  amount 
  category
  occurance
}

mutation createExpense(options:options){
  income {
    id
    amount 
    category
    occurance
  }
}

mutation updateExpense(id:id, options:options){
  income {
    id 
    amount
    category
    occurance
  }
}'

mutation deleteExpense(id:id){
  boolean
}