query findIncome(id:id) {
  id
  amount 
  category
  occurance
}

mutation createIncome(options:options){
  income {
    id
    amount 
    category
    occurance
  }
}

mutation updateIncome(id:id, options:options){
  income {
    id 
    amount
    category
    occurance
  }
}'

mutation deleteIncome(id:id){
  boolean
}